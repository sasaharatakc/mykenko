{
  "name": "@mykenko/db",
  "version": "0.0.1",
  "private": true,
  "description": "Shared PostgreSQL schema and query helpers (Drizzle ORM)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.10",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.21.4",
    "typescript": "^5.4.5"
  }
}
